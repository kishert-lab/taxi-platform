server:
  host: 0.0.0.0
  port: 8080

database:
  host: localhost
  port: 5432
  user: taxi
  password: taxi_password
  dbname: taxi
  sslmode: disable

redis:
  host: localhost
  port: 6379

jwt:
  secret: super_secret_key
  access_ttl: 24h